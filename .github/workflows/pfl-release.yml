name: PFL bor create new release

on:
  workflow_dispatch:

jobs:
  execute-code:
    runs-on: pfl-main-ubuntu

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Run Code
        run: |
          git status
          git remote -v
          git remote add upstream git@github.com:Santonclause/tempupstream.git
          git tag
          git status
          git remote -v
          git fetch --tags
          git tag



